# 🤝 깃 협업 가이드 및 규칙

## 1. 브렉치 전략 (Git Flow Lite)
- **main**: 배포 가능한 최신 상태 (직접 Push 금지)
- **feature/**: 기능 개발 (예: `feature/motor-control`)
- **fix/**: 보그 수정 (예: `fix/sensor-error`)

## 2. 커밋 메시지 컨벤션
`<타입>: <제목>` 형식을 따릅니다.

| 타입 | 설명 |
| :-- | :-- |
| **feature** | 새로운 기능 추가 |
| **docs** | 버그 수정 |
| **docs** | 문서 수정 |
| **style** | 코드 포맷팅 (로직 변경 X) |
| **refactor** | 리펙토링 |

## 3. 협업 워크플로우
1. `git pull origin main` (최신화)
2. `git checkout -b feature/기능명` (브랜치 생성)
3. 작업 후 `git push origin feature/기능명`
4. GitHub에서 **Pull Request (PR)** 생성
