# YYYY-MM-DD Jetson 작업 일지 템플릿

## 결론

- 오늘 `Jetson`에서 가장 중요한 결과는 무엇이었나?
- `SSH` 기준으로 해결한 것과, 직접 모니터를 붙여야만 확인된 것은 무엇이었나?
- 다음 작업의 1순위는 무엇인가?

## 오늘 작업 한 줄 요약

- 오늘 `Jetson`에서 무엇을 했는가?
- 왜 이 작업을 먼저 했는가?

## 현재 작업 형태

- `SSH`만으로 진행했는가, 아니면 `모니터 + 키보드 + 마우스`를 직접 연결했는가?
- 직접 연결이 필요했던 이유는 무엇이었는가?

## 시간순 기록

### HH:MM

- 어떤 작업을 했는가?
- 어떤 명령어를 실행했는가?
- 어떤 결과를 관찰했는가?

```bash
# 실행한 명령어
```

### HH:MM

- 예상과 실제 결과가 어떻게 달랐는가?
- `Jetson`에서만 보인 문제는 무엇이었는가?
- 다음으로 무엇을 확인했는가?

```bash
# 실행한 명령어
```

## 오늘 관찰한 핵심 현상

- `Jetson`에서 `CPU`, `memory`, `temperature`, `GUI`, `USB`, `device busy` 관점에서 무엇이 보였는가?
- `D435i`, `RTAB-Map`, `RViz`, `rtabmap_viz`의 체감 차이는 어땠는가?

## 원인 가설

- 처음에는 원인을 무엇이라고 생각했는가?
- `PC`에서의 가설과 `Jetson` 현장 관찰 결과가 어떻게 달랐는가?

## 해결 방법

- 실제로 적용한 해결 방법은 무엇인가?
- 그 방법이 왜 `Jetson`에서 효과가 있었다고 판단했는가?

## 오늘 만든/수정한 파일

- [파일명](/absolute/path/to/file)

## 증빙 자료

- [이미지 또는 로그](/absolute/path/to/file)

## 남은 문제

- 아직 완전히 해결되지 않은 `Jetson` 쪽 문제는 무엇인가?
- 다음에 직접 화면을 보며 다시 확인해야 하는 것은 무엇인가?

## 다음 액션

1. 다음 작업 1순위는 무엇인가?
2. 다음에 실행할 명령어는 무엇인가?
3. 다음 기록에서 꼭 비교해야 할 항목은 무엇인가?

## 한 줄 회고

- 오늘 `Jetson` 작업을 한 문장으로 요약하면?
