# 2026-07-26 Progress

## Summary

- NUCLEO-F446RE의 MDD10A용 motor-output 경로를 `TIM4 CH1/CH2 + DIR GPIO`로 구성했다.
- STM32 핀 단독 DMM 시험에서 boot/idle safe state와 staged output 변화를 확인했다.
- 3S LiPo 12.36 V 조건에서 MDD10A 입력 12.35 V를 측정하고 motor-disconnected 전원 시험을 통과했다.
- 첫 powered/no-motor 시험에서 양 채널의 PWM/DIR가 MDD10A header에서 서로 바뀐 것을 LED 패턴으로 진단했다.
- 배선을 교정한 뒤 M1A, M1B, stop, M2A, M2B, stop의 6-step LED sequence를 확인했다.
- 임시 test macro를 `0U`로 되돌리고 재빌드·플래시한 뒤 모든 MDD10A output LED가 꺼진 safe state를 확인했다.
- MG540-A/B encoder의 5 V rail과 loaded A/B 전압을 측정해, A/B 각각에 15 kΩ signal-to-GND load를 유지하는 조건으로 STM32 hand-rotation 시험 진입 gate를 `CONDITIONAL PASS`로 판정했다.
- 정확한 PWM 파형, 의도한 post-DIR settle, active timeout/DISARM output-zero, encoder LOW·위상·count·방향은 아직 미계측이므로 motor-output과 encoder 통합은 `PARTIAL`로 유지한다.

## Today Work Log

1. STM32 motor-output configuration
   - `PB6 / TIM4_CH1`을 channel 1 PWM으로 설정했다.
   - `PB7 / TIM4_CH2`를 channel 2 PWM으로 설정했다.
   - `PC8`, `PC9`를 각 channel의 DIR GPIO로 설정했다.
   - TIM4 period는 `4199`, test duty cap은 `100 permille = 10%`로 구성했다.
   - `motor_output_stop_all()`과 direction-change 시 `compare zero -> 1 ms wait -> DIR write -> PWM restore` 순서의 raw output path를 추가했다.
   - 이 주파수와 시간은 source/CubeMX 설정값이며 실제 파형 계측값은 아니다.

2. STM32 pin-only DMM validation
   - MDD10A main power와 motor를 분리한 상태에서 `PB6`, `PB7`, `PC8`, `PC9`를 확인했다.
   - Boot/idle 상태에서 네 핀 모두 0 V를 확인했다.
   - 임시 button-driven 6-step sequence의 PWM/DIR 상태 변화와 stop 복귀를 DMM으로 확인했다.

3. MDD10A standalone power-input validation
   - Battery pack voltage는 12.36 V였다.
   - MDD10A `B+`와 `B-` 사이 입력은 12.35 V였다.
   - Motor는 연결하지 않았고 heat, smell, smoke, abnormal noise 또는 fuse 문제는 없었다.
   - Switch OFF 직후 `-0.24 V`, 이후 `-0.14 V`가 관측되며 0 V 방향으로 천천히 감쇠했다. 저장 커패시터/부유 기준점 영향으로 추정하지만 원인은 확정하지 않았고, 이 값만으로 역전원 여부를 판정하지 않았다.

4. Initial PWM/DIR wiring fault diagnosis
   - 초기 배선에서는 step 1과 4에 LED가 켜지지 않았다.
   - Step 2에서는 M1A가 밝고 M1B가 약하게, step 5에서는 M2A가 밝고 M2B가 약하게 켜졌다.
   - 이 증상으로 양 MDD channel의 PWM/DIR가 서로 바뀐 것을 확인하고 시험을 중단했다.
   - MDD10A 실크 인쇄를 기준으로 두 signal pair를 교정했다.

5. Corrected powered-no-motor sequence
   - Step 1: M1A LED active
   - Step 2: M1B LED active
   - Step 3: all related LEDs off
   - Step 4: M2A LED active
   - Step 5: M2B LED active
   - Step 6: all output LEDs off
   - Motor output terminal은 전 과정에서 비워 두었다.

6. Test closeout
   - Final bench mapping을 `PB6 -> PWM1`, `PC8 -> DIR1`, `PB7 -> PWM2`, `PC9 -> DIR2`, common GND로 확정했다.
   - `MOTOR_OUTPUT_PIN_TEST_ENABLED`를 `0U`로 되돌렸다.
   - 사용자가 rebuild/flash 후 powered-no-motor 상태에서 모든 output LED가 꺼졌음을 확인했다.
   - 시험 종료 사진에는 battery와 motor terminal이 분리된 상태가 보인다.

7. MG540 encoder signal-safety measurement
   - 두 motor를 bench 식별명 `MG540-A`, `MG540-B`로 구분했으며 차량 left/right는 아직 지정하지 않았다.
   - Encoder PCB connector pad 순서를 정면/connector-up 기준 왼쪽부터 `motor+`, `GND`, `B`, `A`, `5V`, `motor-`로 식별했다.
   - MG540-A의 무전원 encoder 5 V-GND 저항은 824 Ω이었고 A-GND, B-GND와 A-B는 DMM `1`/OL이었다.
   - XL4015 #2 rail은 MG540-A 연결 전 5.06 V, 연결 후 5.03 V였고 MG540-B 연결 상태에서도 5.03 V였다.
   - MG540-A raw A/B는 shaft 정지 위치에 따라 약 0 V 또는 약 5 V가 됐다. 따라서 raw direct STM32 연결은 금지했다.
   - MG540-A는 10 kΩ signal-to-GND 조건에서 A/B 정지 HIGH가 모두 2.48 V였고 회전 중 DMM 평균은 각각 1.23 V, 1.24 V였다.
   - MG540-A의 15 kΩ 조건에서 A는 회전 평균 1.48 V, 정지 HIGH 2.98 V였으며 B는 거의 같은 동작만 확인하고 정확값은 기록하지 않았다.
   - MG540-B의 15 kΩ 조건에서 A는 1.46/2.97 V, B는 1.47/2.96 V였다. 앞 값은 회전 DMM 평균, 뒤 값은 정지 HIGH다.
   - Loaded HIGH는 약 10.3~10.5 kΩ internal pull-up과 일치하지만 open-collector/open-drain 구조를 확정하는 증거로 확대하지 않았다.

## Evidence

- 교정 전 배선: [`../../assets/photos/mdd10a/2026-07-26_01_mdd10a_pwm_dir_swapped_before_fix.jpg`](../../assets/photos/mdd10a/2026-07-26_01_mdd10a_pwm_dir_swapped_before_fix.jpg)
- 교정 후 배선: [`../../assets/photos/mdd10a/2026-07-26_02_mdd10a_corrected_pwm_dir_wiring_pass.jpg`](../../assets/photos/mdd10a/2026-07-26_02_mdd10a_corrected_pwm_dir_wiring_pass.jpg)
- MDD10A logic validation: [`../../02_Hardware_Validation/03_MDD10A_Logic_Input_Test.md`](../../02_Hardware_Validation/03_MDD10A_Logic_Input_Test.md)
- Power bring-up record: [`../../02_Hardware_Validation/01_Power_Bringup_Checklist.md`](../../02_Hardware_Validation/01_Power_Bringup_Checklist.md)
- Motor output implementation: [`../../03_Firmware/stm32_uart_mvp/Core/Src/motor_output.c`](../../03_Firmware/stm32_uart_mvp/Core/Src/motor_output.c)
- Temporary test sequence and final macro state: [`../../03_Firmware/stm32_uart_mvp/Core/Src/main.c`](../../03_Firmware/stm32_uart_mvp/Core/Src/main.c)
- CubeMX timer/pin baseline: [`../../03_Firmware/stm32_uart_mvp/stm32_uart_mvp.ioc`](../../03_Firmware/stm32_uart_mvp/stm32_uart_mvp.ioc)
- Encoder safety measurement: [`../../02_Hardware_Validation/04_Encoder_Signal_Safety_Test.md`](../../02_Hardware_Validation/04_Encoder_Signal_Safety_Test.md)
- Encoder PCB pinout photo: [`../../assets/photos/encoder/2026-07-26_01_mg540_encoder_pcb_pinout.jpg`](../../assets/photos/encoder/2026-07-26_01_mg540_encoder_pcb_pinout.jpg)
- MG540P30_12V label photo: [`../../assets/photos/encoder/2026-07-26_02_mg540p30_12v_motor_label.jpg`](../../assets/photos/encoder/2026-07-26_02_mg540p30_12v_motor_label.jpg)
- Final MVP traceability: [`../verification/05_Final_MVP_Requirements_and_Verification_Matrix_ko.md`](../verification/05_Final_MVP_Requirements_and_Verification_Matrix_ko.md)

## Confirmed Results

| Item | Result | Scope |
| --- | --- | --- |
| STM32 boot/idle PWM/DIR zero | `PASS` | Pin-only DMM, four pins 0 V |
| PWM/DIR signal routing | `PASS` | DMM and MDD LED minimum evidence |
| MDD10A power input | `PASS` | Battery 12.36 V, driver 12.35 V, motor disconnected |
| Corrected channel A/B selection | `PASS` | Six-step powered/no-motor LED sequence |
| PWM/DIR swap | `RESOLVED` | Both channel pairs corrected and retested |
| Final test-disabled safe state | `PASS` | Macro `0U`, all output LEDs off |
| Exact 20 kHz / 10% waveform | `NOT TESTED` | Scope/logic analyzer capture 없음 |
| Current pre-DIR zero interval | `IMPLEMENTED / NOT MEASURED` | Source order: PWM 0 -> 1 ms wait -> DIR -> PWM |
| Intended post-DIR settle | `NOT IMPLEMENTED` | 실제 motor 활성화 전 code correction 필요 |
| Active timeout/DISARM actual output zero | `NOT TESTED` | Production output activation not connected |
| Overall motor-output validation | `PARTIAL` | Static/no-motor scope passed; timing and active safety remain |
| MG540-A encoder rail | `CONDITIONAL PASS` | XL4015 #2 5.06 -> 5.03 V; current/heat 미기록 |
| MG540-B connected encoder rail | `CONDITIONAL PASS` | 5.03 V; 연결 전 전압과 current/heat 미기록 |
| Raw encoder A/B | `UNSAFE FOR DIRECT STM32` | Shaft 위치에 따라 약 0/5 V |
| 15 kΩ loaded encoder HIGH | `CONDITIONAL PASS` | Exact-recorded channels 2.96~2.98 V |
| Encoder pulse/count/direction | `NOT TESTED` | DMM 평균만 관찰; scope/logic analyzer와 timer count 미실행 |

## Decisions

- MDD10A bench signal mapping은 `PB6/TIM4_CH1 -> PWM1`, `PC8 -> DIR1`, `PB7/TIM4_CH2 -> PWM2`, `PC9 -> DIR2`로 고정한다.
- `left/right`는 현재 firmware channel 이름이며 실제 차량 좌우 mapping은 motor 장착 후 확정한다.
- 실제 차량 forward/reverse 방향도 motor 연결 전에는 확정하지 않는다.
- 임시 raw output test는 10% 상한을 유지하고 시험 종료 시 반드시 macro를 `0U`로 되돌린다.
- LED와 DMM 결과는 static routing 검증으로만 인정하며 주파수, duty와 deadtime 검증으로 대체하지 않는다.
- 실제 motor 연결 전 active timeout/DISARM output-zero gate를 반드시 통과한다.
- Encoder motor는 당분간 `MG540-A/B`로만 부르고 차량 left/right는 장착 후 확정한다.
- Raw 5 V encoder A/B는 STM32에 직접 연결하지 않는다. 첫 hand-rotation count 시험은 A/B 각각의 15 kΩ signal-to-GND load와 common GND를 필수 조건으로 한다.
- 330 Ω~1 kΩ 입력 series resistor는 후보이며 최종 값과 실제 배선은 다음 단계에서 검증한다.

## Blockers / Open Items

- Oscilloscope 또는 logic analyzer를 이용한 실제 20 kHz와 10% duty 측정이 없다.
- 현재 source는 `PWM zero -> 1 ms wait -> DIR change -> 즉시 PWM resume` 순서다. 의도한 post-DIR settle로 고치고 실제 timing을 확인해야 한다.
- UART command를 제한된 motor-output 값으로 변환하는 production mapping이 아직 완료되지 않았다.
- Active PWM 상태의 timeout, DISARM과 fault에서 실제 핀/MDD output-zero 시험이 없다.
- Encoder의 정확한 LOW, pulse shape, A/B phase, timer count와 방향 부호는 아직 확인되지 않았다.
- 실제 motor no-load, current, heat, noise와 vehicle left/right/forward mapping은 아직 시험하지 않았다.

## Next Actions

1. 현재 CubeMX/firmware와 encoder voltage evidence를 Git 기준점으로 보존한다.
2. TIM3 `PB4/PB5`를 첫 encoder channel 후보로 설정하고, 채널별 15 kΩ load와 common GND 조건에서 motor power 없이 hand-rotation count를 확인한다.
3. TIM3 count와 방향이 확인되면 TIM5 `PA0/PA1`로 두 번째 channel을 반복한다.
4. 실제 motor 활성화 전에 direction sequence를 `PWM zero -> DIR -> settle -> PWM`으로 수정하고 계측 가능한 범위에서 확인한다.
5. UART command state를 10% 제한 motor-output interface에 연결하고 active timeout/DISARM/fault output-zero를 검증한다.
6. 위 gate를 통과한 뒤에만 한쪽 motor lifted/no-load 시험을 진행한다.
