# 2026-08-06 Progress

## Summary

- STM32 wrong-ACK one-shot hook을 `1U`에서 `0U`로 복구했다.
- ESP32 scripted motion과 STM32 UART/motor/fault 관련 test hook이 모두 `0U`인 safe source를 재확인했다.
- Firmware contract test `15/15`와 STM32 CubeIDE build `0 errors / 0 warnings`를 통과했다. 별도 board log의 observed safe UART behavior도 통과했다.
- 두 UART monitor 로그를 evidence 폴더에 보존하고 결과·해시·증거 한계를 인덱스에 기록했다.

## Source And Static State

| Item | 2026-08-06 state |
| --- | --- |
| ESP `BRIDGE_SCRIPTED_TEST_ENABLED` | `0U` |
| ESP `TEST_STEP_PERIOD_MS` | `1000` |
| STM `UART_MVP_OUTPUT_TEST_ENABLED` | `0U` |
| STM stale DISARM ACK injection | `0U` |
| STM wrong DISARM ACK type injection | `0U` |
| STM stale/suppressed PONG injection | `0U` |
| STM motor-output/fault button hooks | `0U` |
| Python firmware contract | `Ran 15 tests`, `OK` |

## Build Evidence

The user built the restored STM32 source in STM32CubeIDE and reported `0 errors / 0 warnings`.
The resulting local artifact was inspected after the build:

| Artifact | Value |
| --- | --- |
| Restored source modified | `2026-08-06 16:41:45.562` |
| Protocol object modified | `2026-08-06 17:02:36.469` |
| Path | `03_Firmware/stm32_uart_mvp/Debug/stm32_uart_mvp.elf` |
| Modified | `2026-08-06 17:02:37.995` |
| Size | `1,239,972 bytes` |
| SHA-256 | `71EF2C275A5DD5CFAB34995D1CF33A76B4DC4593661842BD6E379D6DBEFACBAF` |

The source modification time precedes the object/ELF timestamps, which is consistent with the restored source being compiled into this local build. These timestamps are supporting provenance, not independent proof of compiler inputs or of the exact binary running on the board. A flash transcript was not preserved.

## Runtime Evidence

| Run | Result |
| --- | --- |
| [`2026-08-06_safe_image_uart_runtime_regression_run1_10s_total.txt`](../../assets/logs/esp32_uart_bridge/2026-08-06_safe_image_uart_runtime_regression_run1_10s_total.txt) | Exact ACK/PONG/READY, TEL 100/100 safe and errors 0; post-READY 9.45 s, so the 10 s dwell criterion was short by 0.55 s |
| [`2026-08-06_safe_image_uart_runtime_regression_pass.txt`](../../assets/logs/esp32_uart_bridge/2026-08-06_safe_image_uart_runtime_regression_pass.txt) | Exact ACK/PONG/READY, post-READY 11.35 s, TEL 120/120 `DISARMED/zero/error 0`, ARM/CMD and errors 0 — PASS |

The final run used `DISARM seq=1291886514`, matching `ACK,type=DISARM`, then `PING/PONG seq=1291886515`. `STARTUP READY` appeared at ESP log `887 ms`; the final TEL appeared at `12237 ms`.

## Evidence Boundary

- The raw logs prove the observed UART order and telemetry values only.
- The logs do not independently record LiPo/MDD10A/motor-power separation or UART rewiring conditions.
- The logs do not contain a flash transcript or embedded binary identity, so exact ELF-to-board linkage remains pending.
- This result does not verify MDD10A power output, actual motor stop, reset-marked boot, Physical E-stop or electrical timing.

## Decisions

- The 2026-08-06 current safe source/static/build is `PASS`; the separately observed UART runtime behavior is also `PASS` within its stated scope.
- The UART release remains `PARTIAL` because Gate C ESP-response and STM32-command parser recovery are not tested.
- Gate C begins only from this all-hooks-`0U` checkpoint and with LiPo, MDD10A B+/B- and actual motor power disconnected.

## Git Scope

The safe-baseline change set is scoped to the one-line wrong-ACK hook restore, dated evidence and documentation. CubeIDE-generated local changes in `.settings/language.settings.xml` and `stm32_uart_mvp Debug.launch` are unrelated and remain excluded.

## Next Actions

1. Execute the first T-BRIDGE-008A vector: reject a duplicate-`seq` DISARM ACK, retry the same DISARM sequence after 500 ms and recover only on the exact ACK/PONG.
2. Complete the remaining ESP response-parser malformed vectors without ARM/CMD traffic.
3. Execute T-BRIDGE-008B malformed STM32 command rejection and final valid PING/PONG recovery.
4. Restore every controlled hook to `0U`, rerun `15/15`, rebuild/reflash and repeat the safe runtime regression after controlled testing.
5. Continue with command-timeout and software-fault shutdown latency, reset-marked boot, board power/back-power and Physical E-stop gates before any actual motor test.
