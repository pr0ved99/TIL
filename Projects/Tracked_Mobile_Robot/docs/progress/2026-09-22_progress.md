# 2026-09-22 Progress

## 종료 지점

**T-ESTOP-004 모터 분리 조건의 S0-B/opto/PC7→펌웨어→PWM 시험 PASS.**
정상 부팅, S0 입력 차단, active RESET 거절, 해제 후 latch, explicit RESET, ARM-only zero,
fresh CMD 후 재출력·DISARM, pressed/open boot, 동작 중 감지선 단선과 all-hooks-0U 복구를 확인했다.
정본은 [report 26](../verification/26_T_ESTOP_004_Conditioned_PWM_Latch_Reset_and_Safe_Restore_Test_Report_2026-09-22_ko.md)이다.
K1/MDD10A 전력 차단·실모터·전체 Physical E-stop PASS는 아니다.

## 오늘 변경과 증거

- 사용자가 STM32/ESP32 빌드·플래시와 모든 배선·계측을 수행했다. Codex는 소스 확인,
  raw digital sample/UART 분석, Python 검사와 문서 정리만 수행했다.
- left_cps ±10: COM3에서 TIM3 ±1 raw count와 일치. PB4/PB5 각각 임시 15 kΩ GND
  pull-down 후 원시 ENC 414쌍에서 left delta/CPS=0. 펌웨어 계산 변경 없음.
- actual VRT의 T004 측정 헤더 Net/위치를 확인했다. 전체 최신 Wire graph/PDF release까지
  재검토한 것은 아니다. CTRL/ENC는 각각 3핀 두 개다.
- run02 S0 차단·active RESET 거절, run03 같은 부팅의 latch/reset/fresh-command/final DISARM,
  run04 S0-active boot, run05 JESTOP Pin3-open boot, run06 active wire-open PASS.
- run06 PC7 HIGH→두 PWM last falling edge **357.25 µs**, 이후 **13.7398925 s LOW**.
  run02/03은 LOW 위상에서 assertion되어 last-fall 차이가 음수다. 음수/0 µs 응답시간으로 보고하지 않는다.
- 사용자가 ESP T004 한 줄을 **1U→0U**로 복구. STM 코드 변경 없음. 최종 STM 빌드·플래시
  성공 사용자 확인, ESP 새 ELF hash와 USB monitor 일치. 정적 검사 **30/30 PASS**.
- run07: 25 s 두 PWM HIGH 0개, TEL **199개 모두 DISARMED/0**, err/drop=0.
  UART DISARM/PING 각 1회, ARM/CMD/ESTOP_RESET 송신 0회.
- [증거 묶음](../../assets/logs/estop/2026-09-22_t004/README.md)에 decode/summary/manifest,
  초기·복구 ESP monitor와 encoder 전후 로그를 보존했다. raw `.sr/.pvs`는 기존 위치에 유지했다.
  run01 이름 있는 파일은 1.25 s이며 이전 25 s `Session 2`는 교체됐다. 부팅 정식 증거는 run03이다.

## 종료 상태와 한계

- MDD10A B+·두 모터·BNO 모듈은 분리 유지. S2 조작 없음.
- JESTOP Pin3 임시 직렬 연결 제거·원복을 안내했고 사용자가 다음 단계로 진행했다.
  run07 정상 PC7 LOW가 감지 경로 복구의 powered 근거다.
- 최종 runtime: 보드 USB 제거, JP5=E5V, JP1=OPEN, #1 두 2P 및 #2 J3 연결.
  종료 안내는 S1 OFF·LiPo 분리. 종료 뒤 실제 무전원 상태를 캡처가 증명하는 것은 아니다.
- 왼쪽 임시 15 kΩ pull-down 유지. 오른쪽은 각 capture 부팅 때 +10 cps 1회가 남아 있으므로
  영구 입력 조정부·powered encoder 검증을 완료로 처리하지 않는다.
- build/flash 전체 console, 계측기 모델·교정, controlled BIN 사본은 미보존이다.
  이전 hash·사용자 보고·runtime 증거의 경계를 report 26에 명시했다.
- 오늘 문서·증거 정리는 한 번에 수행했다. 이후 사용자가 Git 최신화를 요청해 ESP hook0,
  raw/decoded 증거, 보고서·progress·handoff·다음 세션 계획을 같은 체크포인트에 포함한다.

## 다음 시작점

1. 완료한 T004 시험이나 firmware 입력을 반복하지 않는다. 모든 controlled hook=0U를 유지한다.
2. 전원 분배 허브, F1의 257/287 식별, K1 14 AWG와 280756-4 단자 허용 AWG,
   F2/배선/커넥터 release 미결 항목을 기존 문서와 실물 기준으로 정리한다.
3. 그 결과를 바탕으로 **T-ESTOP-005A — 모터 분리 상태의 실제 MDD10A 입력 rail 차단,
   no-auto-restart/back-power 검증**을 준비한다. 아직 B+나 모터 연결 승인이 아니다.
4. 이후 실물 장착, 첫 모터 저듀티·encoder noise/sign, 배터리 ADC·주행/odometry 순서로 진행한다.

## 휴식 후 재개 계획

사용자가 수면 후 재개하기로 했다. [다음 세션 계획](../plans/2026-09-22_Next_Session_Power_Path_and_T_ESTOP_005A_Plan_ko.md)을 작성했다.
첫 작업은 현재 +/− 분배부·F1/F2·K1 단자/선재를 문서상 미결 항목과 대조하는 것이다.
진입 조건이 닫히면 모터 분리 상태에서 MDD10A 실제 입력의 정적 rail 차단·재투입 시험으로
이어간다. 계획 작성만 완료했으며 부품 변경·전력단 연결·새 시험을 수행하지 않았다.

## Git 저장 중 추가된 도면

- T004 evidence/hook0/다음 계획은 `ba692ef`로 커밋하고 같은 브랜치 원격에 push했다.
- 최종 상태 확인 중 04:18:39 저장된 현재 CTRL수정본 VRT 변경이 발견돼 추가 체크포인트에 보존한다.
  크기 160,699 bytes, SHA-256 `26bc7f6cfc48d415185a1c40590c181038191a3e3d91653f68ed7e75e1a8c666`.
- 시험 시 사용한 직전 파일은 `96f881a5...c36aa9`다. 새 파일의 회로/Net/배선 검토는 수행하지 않았다.
  T004 PASS를 새 도면의 검토 완료로 확대하지 않으며 다음에 해당 도면을 사용할 때 관련 변경을 확인한다.
