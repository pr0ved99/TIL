# 2026-08-04 Progress

## Summary

- 2026-08-03 response-gated startup 원본 로그 6개를 다시 읽어 Gate A happy path와
  Gate B DISARM-ACK/PONG bounded failure, stale-sequence rejection, controlled reset
  recovery를 검증했다.
- Gate A와 Gate B 핵심 runtime behavior는 PASS다. `wrong ACK type`은 별도 주입
  로그가 없어 T-BRIDGE-007 전체 coverage는 PARTIAL이다.
- READY 이후 controlled normal sequence와 active DISARM은 PASS했지만 Gate C의
  malformed PING/CMD/unknown frame 거부 뒤 valid PING/PONG recovery는 아직
  NOT TESTED다.
- 4 MHz, 20 M-sample raw capture에서 active DISARM LF 수신 완료부터 PB6/PB7의
  마지막 active edge까지 `23.50 us`를 측정했다. PWM은 STM32 ACK 시작보다
  `62.75 us` 먼저 멈췄고 캡처 끝까지 재개되지 않았다.
- 이 active-DISARM 결과는 STM32 MCU logic pin 하위 게이트만 PASS다. MDD10A
  전력단, 실제 motor stop과 Physical E-stop의 증거가 아니다.
- Current worktree는 ESP script `0U/1000 ms`, STM32 UART output hook `0U`로
  safe-restored됐다. Contract `15/15`와 isolated clean STM32/ESP32 build run
  `20260804043010-26408-7918`도 PASS지만 safe-image board reflash/run은 pending이다.

## UART Runtime Results

| Vector | Key observation | Result |
| --- | --- | --- |
| Gate A | exact `DISARM S/ACK S,type=DISARM -> PING S+1/PONG S+1 -> READY`; ARM/CMD 0 | PASS |
| Missing DISARM ACK | same seq, 500 ms intervals, 3 TX, then `STARTUP FAILED`; ARM/CMD 0 | PASS |
| Missing PONG | DISARM ACK success, same PING seq 3 TX, then `STARTUP FAILED`; ARM/CMD 0 | PASS |
| Stale ACK seq | `S-1` ignored, retry, exact ACK only then PING/READY | PASS |
| Stale PONG seq | previous seq ignored, retry, exact PONG only then READY | PASS |
| Wrong ACK type | matching seq with wrong type not separately injected | NOT TESTED |
| Controlled ESP reset/new startup | new `S/S+1` startup reaches READY; ARM/CMD 0 | PASS behavior; post-failure session linkage pending |
| Gate C normal sequence | NOT_ARMED, ARM, valid CMD, OUT_OF_RANGE, active DISARM | PASS |
| Gate C ESP response recovery | malformed response rejected, then exact response accepted | NOT TESTED |
| Gate C STM32 command recovery | malformed/unknown command rejected, then valid PING/PONG | NOT TESTED |

All six response-gated logs contain zero ARM/CMD TX and every parsed telemetry row is
DISARMED with zero velocity. The filenames containing `gate_c1/c2` are preserved as raw
evidence names, but their actual role is Gate B wrong/stale-response rejection rather
than Gate C malformed-command recovery.

## Active DISARM Measurement

| Point | Time |
| --- | ---: |
| DISARM final LF stop-bit end | `2,287,888.50 us` |
| PB6/PB7 last active falling edge | `2,287,912.00 us` |
| STM32 ACK start | `2,287,974.75 us` |
| DISARM-to-PWM-zero baseline | `23.50 us` |
| PWM stop before ACK | `62.75 us` |

Capture channel map:

- D0 PC8/DIR1
- D1 PB6/PWM1
- D2 PC9/DIR2
- D3 PB7/PWM2
- D4 PA10/USART1 RX
- D5 PA9/USART1 TX
- analyzer GND to STM32 GND

## Current Source And Static Test State

Actual source values reread after the 2026-08-04 safe restore:

- ESP32 `BRIDGE_SCRIPTED_TEST_ENABLED=0U`
- ESP32 `TEST_STEP_PERIOD_MS=1000`
- STM32 `UART_MVP_OUTPUT_TEST_ENABLED=0U`
- STM32 `UART_MVP_STALE_DISARM_ACK_ONCE_TEST_ENABLED=0U`
- STM32 `UART_MVP_STALE_PONG_ONCE_TEST_ENABLED=0U`
- STM32 `UART_MVP_SUPPRESS_PONG_TEST_ENABLED=0U`
- STM32 `MOTOR_OUTPUT_PIN_TEST_ENABLED=0U`
- STM32 `MOTOR_FAULT_INJECTION_TEST_ENABLED=0U`

Current contract test execution:

```text
Ran 15 tests
OK
```

The current restored source passes all 15 contract tests. Isolated clean STM32 and
ESP32 builds also passed in run `20260804043010-26408-7918`. These are source/static/build
results, not proof that the restored images were flashed or run on either board.

## Evidence

- [Response-gated startup report](../verification/09_ESP32_STM32_UART_Response_Gated_Startup_Test_Report_2026-08-03_ko.md)
- [Active DISARM latency report](../verification/10_STM32_Active_DISARM_Shutdown_Latency_Test_Report_2026-08-04_ko.md)
- [UART raw-log index](../../assets/logs/esp32_uart_bridge/README.md)
- [Logic-analyzer raw-capture index](../../assets/captures/logic_analyzer/README.md)

## Evidence Boundary And Pending Operator Confirmation

Raw logs and `.sr` capture prove the observed digital behavior. They do not independently
record the following physical preconditions:

- LiPo disconnected
- MDD10A B+/B- and actual motor power disconnected
- UART signal changes performed only with both boards powered off
- analyzer connected only to STM32 3.3 V logic pins and common GND

Until the operator confirms them, the reports keep these fields as
`operator confirmation pending`. Flash transcript and binary hash are also absent, so
the exact flashed image identity is not independently proven by the raw files.

## Decisions

- Gate A runtime behavior is PASS.
- Gate B bounded no-response failure is PASS; stale sequence rejection and new-startup
  recovery are also PASS.
- T-BRIDGE-007 remains PARTIAL only because wrong ACK type has no separate runtime vector.
- Gate C remains PARTIAL because ESP response and STM32 command parser recovery are open.
- Active DISARM MCU-pin baseline is PASS at 23.50 us; MVP-009 and the overall motor
  safety gate remain PARTIAL.
- No motor-energy source may be connected until the restored safe images are reflashed/run
  and ARM/CMD 0 behavior is preserved; the board image may still be the controlled-test build.

## Next Actions

1. Preserve the completed safe source restore, contract `15/15` and isolated clean build
   run `20260804043010-26408-7918`.
2. Flash/run the restored safe images with LiPo, MDD10A B+/B- and actual motor power
   disconnected; confirm READY after matching responses and no scripted ARM/CMD.
3. Capture required wrong ACK type rejection, then execute both Gate C parser recovery
   directions defined by T-BRIDGE-008A/B.
4. Capture command-timeout latency, then software-fault latency; restore safe hooks and
   perform the reset-marked boot regression after each controlled test cycle.
